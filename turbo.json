{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [
    ".env*",
    "pnpm-lock.yaml",
    "tsconfig.json",
    "eslint.config.mjs"
  ],
  "tasks": {
    "prepack": {
      "dependsOn": ["^prepack"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["prepack"],
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^prepack"],
      "inputs": ["$TURBO_DEFAULT$", "**/*.test.ts", "vitest.config.ts"],
      "outputs": ["coverage/**"]
    }
  },
  "ui": "tui"
}
